1. Should I use scalar or swagger?
2. Whats the whole point with documentation?
3. How do I do tests? What types exist?
4. How enums work with models and why theyre not part of DataBase?
5. 





PRIORITY 
1. Make CRUD really work.
I mean test it in Swagger

1.1 have CRUD in all 3 controllers

1.2 Make DTOs

2. >maybe< try to understand the relations here.

3. Try adding frontend from Refine.



 CRUD are 4 operations, each = distinct endopoint
C - Create  → POST   /api/users          (endpoint 1)
R - Read    → GET    /api/users/{id}     (endpoint 2)
R - Read    → GET    /api/users          (endpoint 3) lista wszystkich
U - Update  → PUT    /api/users/{id}     (endpoint 4)
D - Delete  → DELETE /api/users/{id}     (endpoint 5)


Path through Repository/Service/Controller = sharing responsibility
<if u have services then u dont test controllers (its optional) cuz services have logic>

Frontend (JS/React) 
   ↓ HTTP GET/POST/PUT/DELETE
Controller (UsersController)
   ↓ calling service
Service (UserService / IUserService)
   ↓ calling repository
Repository (UserRepository / IUserRepository)
   ↓ data base access (EF Core)



Another example (focus on services and repositories)

   Controller
   |
   v
Service (IVehicleService -> VehicleService)
   |
   v
Repository (IVehicleRepository -> VehicleRepository)
   |
   v
Database (AppDbContext / EF Core)